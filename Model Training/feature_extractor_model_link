# Feature Extraction Model (MobileNetV2)

This file contains the link to download the pre-trained Feature Extraction Model used for drowsiness detection.

Due to GitHub file size limitations, you can download the model from the link below:

- [Download feature_extractor_model.h5](https://drive.google.com/file/d/1-6vweRY4SCL5NNh2nkQqX4V2ZJECyfwp/view?usp=sharing)

---

## How to Load the Model:
```python
from tensorflow.keras.models import load_model

FEATURE_EXTRACTOR_PATH = "feature_extractor_model.h5"

# Load the saved feature extraction model
feature_extractor = load_model(FEATURE_EXTRACTOR_PATH)

print("Feature Extraction Model Loaded Successfully!")
